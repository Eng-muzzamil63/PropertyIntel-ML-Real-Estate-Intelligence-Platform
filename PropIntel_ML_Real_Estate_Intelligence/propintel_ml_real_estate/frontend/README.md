# PropIntel Frontend

Premium dark analytics dashboard for the PropIntel ML backend.

## Run

```powershell
npm install
npm run dev
```

Open `http://localhost:3000`.

Set `NEXT_PUBLIC_API_URL` in `.env.local` if the backend is not on `http://127.0.0.1:8000`.
